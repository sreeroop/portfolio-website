import Head from 'next/head'
import Image from 'next/image'
import { AppBar, Typography, Grid, Box } from '@mui/material'
import styles from '../styles/Home.module.css'
import ProjectCard from '../components/ProjectCard'
import Tools from '../components/Tools'
import ToolStrip from '../components/ToolStrip'
import ExperienceStrip from '../components/ExperienceStrip'
import Landing from '../components/Landing'
import { experiences, tools } from '../data/data'



export default function Home() {

  return (
    <>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <AppBar sx={{ position: 'fixed', left: 0, top: 0, display: 'flex', justifyContent: 'center', height: '10vh', width: '100vw' }}>
        <Typography variant='h5' sx={{ width: '200px' }}>Sreeroop</Typography>
      </AppBar>
      <Grid container sx={{ display: 'flex', flexDirection: 'column', maxWidth: '100vw', overflowX: 'hidden' }}>
        <Grid item sx={{ minHeight: '100vh', }}>
          <Landing />
        </Grid>

        <Grid item sx={{ minHeight: '100vh', width: '110vw', margin: 'auto' }}>
          {
            experiences.map((experience) => {
              return <ExperienceStrip data={experience} />
            })
          }
        </Grid>
        <Grid item sx={{ minHeight: '100vh', width: '110vw', margin: 'auto' }}>
          <div className={`${styles.marquee} ${styles['marquee--fit-content']} ${styles['marquee--hover-pause']} ${styles['enable-animation']}`}>
            <ul className={styles.marquee__content}>
              {
                tools.map(tool => {
                  return (
                    <li>
                      <ToolStrip name={tool.name} color={tool.color} />
                    </li>
                  )
                })
              }
            </ul>

            <ul aria-hidden="true" className={styles.marquee__content}>
              {
                tools.map(tool => {
                  return (
                    <li>
                      <ToolStrip name={tool.name} color={tool.color} />
                    </li>
                  )
                })
              }
            </ul>
          </div>
          {/* <marquee style={{ width: '90vw' }}>
           <Box sx={{ display: 'flex', flexDirection: 'row', alignItems: 'center', justifyContent: 'space-evenly', }}>

           </Box>
        </marquee> */}
        </Grid>

        <Grid item sx={{ minHeight: '100vh' }}>
          <ProjectCard />
        </Grid>


      </Grid >
    </>
  )
}
